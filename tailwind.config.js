/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // <--- Add this line or change it if it's different
  content: [
    "./src/**/*.{js,jsx,ts,tsx}"
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

